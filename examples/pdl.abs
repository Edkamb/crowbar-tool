module Dummy;

data NewSpec = Demonic | Prob(Rat);

[Spec: Ensures(x == 2)]

{
Int x = 0;

[Spec: Demonic]
if( True ) { x = 1; }
else { x = 2; }

/*[Spec: Demonic]
if( True ) { x = x + 1; }
else { x = x + 2; }*/
}